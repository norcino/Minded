The backend of the example application in the folder /Example, contains multiple classes which have not been implemented correctly.
For example business logic has been implemented inside the controllers, or SOLID principles have been broken.
The details of how the implementation should be done is described in the steering file under .github/instructions/minded-backend.instructions.md.

I need to plan the review and refactor of all controllers and classes present in this Example application, ignoring the framework specific files like Program.cs.
First create a table indicating "Identifier", "File name", "Brief violation description summary", "Completed status".
For classes not violating any rule mark "Completed status" as "No action".
I need to process every class one by one and create on task to fix each.